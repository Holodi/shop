# Використання базового образу Python
FROM python:3.8

# Переміщення у робочу директорію в контейнері
WORKDIR /app

# Копіювання файлів з проекту в контейнер
COPY . /app

# Встановлення залежностей
RUN pip install --no-cache-dir -r requirements.txt

# Відкриття порту, на якому буде працювати додаток
EXPOSE 5000

# Команда для запуску додатку
CMD ["python", "app.py"]
